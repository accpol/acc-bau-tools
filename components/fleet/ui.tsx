"use client";

/**
 * The v0.2.0 UI imported the removed PendingFile API.
 * Keep this filename as a compatibility entry point instead of requiring a
 * manual deletion. The current forms and FleetApp use the current UI only.
 */
export * from "./common";
