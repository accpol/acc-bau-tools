"use client";

import FleetPage from "./FleetPage";

/** Retained filename for older links/imports; all login and fleet logic stays in FleetPage. */
export default function FleetApp() {
  return <FleetPage lang="pl" user="" people={[]} projects={[]} />;
}
